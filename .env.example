GEMINI_API_KEY=your_key_here
# PAPERGUARD_GEMINI_MODEL=gemini-3.1-flash-lite   # default; GA cost/latency tier
CROSSREF_EMAIL=your_email@example.com
# OpenAlex (services/openalex.py) needs NO signup/key at all -- it reuses
# CROSSREF_EMAIL above for its "polite pool" (faster, no throttling), or set
# OPENALEX_EMAIL separately if you'd rather use a different contact address.
# OPENALEX_EMAIL=your_email@example.com

# --- Removed 2026-07-16: SEMANTIC_SCHOLAR_API_KEY, SERPER_API_KEY ---
# Semantic Scholar was replaced by OpenAlex (no key needed, see above) after
# hitting persistent 429 rate-limit errors on the public API in testing.
# Serper was removed outright: its free tier requires a signup that was
# unreliable in testing, and no other general web-search API works without a
# paid key as of this writing (checked: Brave dropped its free tier, DuckDuckGo's
# public API only returns "instant answer" boxes, not real search results).
# Plagiarism detection still works via n-gram overlap + semantic embeddings +
# a small known-text fingerprint list -- see agents/plagiarism_agent.py.

# --- Alibaba Cloud / Qwen (DashScope) -- alternative to Gemini ---
# Set DASHSCOPE_API_KEY (and leave GEMINI_API_KEY unset) to run the sub-agent
# LLM calls (citation claim verification, plagiarism similarity, quality prose
# review, reference parsing) on Qwen instead of Gemini. See services/llm.py.
# DASHSCOPE_API_KEY=your_dashscope_key_here
# PAPERGUARD_DASHSCOPE_MODEL=qwen-plus
# PAPERGUARD_DASHSCOPE_API_BASE=https://dashscope-intl.aliyuncs.com/compatible-mode/v1
# PAPERGUARD_LLM_PROVIDER=auto   # auto | gemini | dashscope

# --- CrewAI crew-level LLM (orchestrator synthesis) -- separately configurable ---
# Default: gemini/gemini-3.1-flash-lite. For Alibaba deployment, use:
# PAPERGUARD_CREW_MODEL=dashscope/qwen-plus
# PAPERGUARD_CREW_API_BASE=https://dashscope-intl.aliyuncs.com/compatible-mode/v1

# --- Detector model + threshold overrides (optional) ---
# PAPERGUARD_DETECTOR_MODEL=desklib/ai-text-detector-v1.01
# PAPERGUARD_DETECTOR_AI_THRESHOLD=90
# PAPERGUARD_DETECTOR_HUMAN_THRESHOLD=35

# Shared LLM sampling temperature (verdict/synthesis determinism). Default 0.0.
# PAPERGUARD_LLM_TEMPERATURE=0.0
