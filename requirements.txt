pymupdf4llm
pymupdf
pydantic
google-genai
requests
streamlit
python-dotenv
reportlab

# Qwen / Alibaba DashScope backend for sub-agent LLM calls (OpenAI-compatible
# endpoint; see services/llm.py + services/dashscope_llm.py). Optional: only
# needed if DASHSCOPE_API_KEY is set to route sub-agent calls to Qwen.
openai

# Multi-agent orchestration (agents-as-tools). Gemini is used via LiteLLM.
crewai
crewai-tools

# AI Detection Agent (local PyTorch inference of desklib/ai-text-detector-v1.01,
# a fine-tuned microsoft/deberta-v3-large -- see agents/detector_agent.py).
# CPU-only deployments can install the CPU build of torch. The Detector agent
# degrades gracefully (disables itself) if torch/transformers are unavailable.
# sentencepiece is required for the DeBERTa-v2/v3 tokenizer this model uses
# (the previous DistilBERT model didn't need it).
torch
transformers
accelerate
sentencepiece
